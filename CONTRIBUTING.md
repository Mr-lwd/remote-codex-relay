# 贡献指南

阅读 README、docs/architecture.md 和 docs/development.md。使用独立开发目录与 Codex 测试账号/数据目录，不在真实执行中的实例上运行会改变会话状态的探针。

提交保持范围明确，说明触发条件、修改行为和验证方法。Python 使用 Ruff，JSX/CSS 使用 Prettier；新增配置同步更新示例和配置参考。涉及任务/目标生命周期、权限、附件或会话读取时增加有意义的回归测试，前端调整检查手机视口。

提交前运行格式、静态检查、单元测试、前端构建和与改动相关的浏览器检查。不要提交生成物、运行数据或第三方凭据。不要修改用户全局 Codex 配置来通过测试。

问题反馈和功能建议使用仓库模板；PR 说明实际验证结果。发布和隐私检查步骤见[维护与发布](docs/releasing.md)。不要把完整 `status` 输出或日志直接贴到公开讨论中，先移除自己的域名、地址、路径和会话内容。
