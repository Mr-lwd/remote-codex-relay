# 变更记录

## 未发布

- 补充 `0.0.0.0` 监听、公网 IP / 域名直连、HTTPS 与 Cookie 登录排查说明。
- 登录 Cookie 按实例数据目录隔离，避免旧版同名 Cookie 冲突，并保持切换监听端口时名称稳定。
- 登录后复用会话校验结果；Cookie 被拦截时显示明确提示，避免无提示地停留在登录页。
- 修复非 ASCII 口令或 Cookie 导致认证接口报错，补充认证与浏览器回归测试。

## 1.0.0

首次整理为可独立安装和发布的源码版本。

- 保留多端会话、目标、原生指令/审批、模型选择、Markdown 数学、文件附件与画板。
- 统一 TOML / 环境配置，移除固定用户路径；新安装无会话时正常显示空列表。
- 拆分配置、存储与模型选择模块，格式化 Python / JSX / CSS。
- 锁定依赖，提供安装、环境诊断、原生协议检查、源码打包与通用部署模板。
- 增加隔离测试、GitHub Actions、分层文档与发布排除规则。
