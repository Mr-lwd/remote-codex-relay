# 变更记录

## 未发布

- 修复手机反复开关弹窗和键盘时，内容关闭后全屏暗色遮罩仍延迟退出的问题；关闭画板时保留用户刚选中的输入焦点，避免延迟回调干扰键盘收放。
- 修正网页托管任务的异步问答回传，使用当前任务的 `turn/steer` 接收答案；区分明确拒绝与投递结果不明，避免连接故障后重复发送。
- 接通异步问答选项与答案返回，在输入区上方显示可交互题目；新增服务端 30 秒自动回复、草稿同步、重复提交保护和失败重试，并明确标注系统自动选择。
- 修复手机双指缩放被误判为软键盘、导致整个工作台收缩的问题；忽略重复和不足一个 CSS 像素的视口变化，减少键盘编辑期间的布局抖动。
- 修复关闭侧栏后延迟回调抢走输入焦点的问题，避免随后输入 `n` 误开新任务遮罩；全局快捷键跳过输入法组合输入、菜单、弹窗和已处理的按键。
- 减少输入期间实时同步引发的重复滚动和重绘：跳过无效滚动写入、合并尺寸监听回调，并移除聊天区域的强制绘制隔离；补充中文输入法、连续输入与浏览器帧稳定性检查。
- 回复自动定位改为保持最新回答开头，手动滚动后保留阅读位置；修复回复增长、执行结束和窗口缩放导致跳到底部的问题，并补充历史加载和会话切换的回归验收。
- 修复回复中 `\(...\)`、`\[...\]` 公式无法显示的问题，统一 `$$...$$` 块级排版及 KaTeX 渲染器与样式版本，补充多行公式、列表引用、代码保护和分段返回的浏览器回归验收。
- 增加完整英文 README，并在中英文文档顶部提供语言切换链接。
- 增加绿色终端品牌图标，统一登录页、工作台、浏览器标签页和手机主屏幕快捷方式图标。

## 1.1.0

- 补全 GitHub 反馈模板、依赖更新检查和源码发布流程；扩展发布检查至 Git 索引、历史及自定义隐私标识。
- 排除源码目录中的本地文件、缓存与迁移备份，统一前后端版本信息和通用工作空间名称。
- 增加统一启动、停止、重启、状态、日志和目录迁移修复命令，自动补齐项目依赖并保留失败前的环境与构建。
- 增加中文故障页、服务路径接管校验、端口诊断与维护期间的任务/队列保护。
- 补充 `0.0.0.0` 监听、公网 IP / 域名直连、HTTPS 与 Cookie 登录排查说明。
- 登录 Cookie 按实例数据目录隔离，避免旧版同名 Cookie 冲突，并保持切换监听端口时名称稳定。
- 登录后复用会话校验结果；Cookie 被拦截时显示明确提示，避免无提示地停留在登录页。
- 修复非 ASCII 口令或 Cookie 导致认证接口报错，补充认证与浏览器回归测试。

## 1.0.0

首次整理为可独立安装和发布的源码版本。

- 保留多端会话、目标、原生指令/审批、模型选择、Markdown 数学、文件附件与画板。
- 统一 TOML / 环境配置，移除固定用户路径；新安装无会话时正常显示空列表。
- 拆分配置、存储与模型选择模块，格式化 Python / JSX / CSS。
- 锁定依赖，提供安装、环境诊断、原生协议检查、源码打包与通用部署模板。
- 增加隔离测试、GitHub Actions、分层文档与发布排除规则。
